[package]
name = "controllers-core"
version = "0.1.0"
edition = "2021"

[lib]
name = "controllers_core"
crate-type = ["cdylib"]

[dependencies]
jni = { version = "0.21", default-features = false, features = ["invocation"] }
log = "0.4"
android_logger = "0.14"
bitflags = "2"
